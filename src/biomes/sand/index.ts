import { Biome } from '../biome';

import flat from './flat.png';

// Theme
export default new Biome({
  thickness: 1,
  texture: flat,
  colors: {
    main: '#d7d09d',
    shadow1: '#c2bc8e',
    shadow2: '#97926d',
  }
});
