import { Biome } from '../biome';

import flat from './flat.png';

// Theme
export default new Biome({
  thickness: 1,
  texture: flat,
  colors: {
    main: '#b98b5d',
    shadow1: '#a77d53',
    shadow2: '#b98b5d'
  }
});
