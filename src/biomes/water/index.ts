import { Biome } from '../biome';

import flat from './flat.png';

// Theme
export default new Biome({
  thickness: 0,
  texture: flat,
  colors: {
    main: '#9bd3e1',
    shadow1: '#9bd3e1',
    shadow2: '#9bd3e1',
  }
});
