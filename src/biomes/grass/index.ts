import { Biome } from '../biome';

import flat from './flat.png';

// Theme
export default new Biome({
  thickness: 1,
  texture: flat,
  colors: {
    main: '#8ab549',
    shadow1: '#7ca341',
    shadow2: '#5f7e30'
  }
});
